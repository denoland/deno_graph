md/cli/0.0.0
-- deno.lock --
{
  "version": "3",
  "remote": {}
}

===

== FAST CHECK EMIT PASSED ==

== TYPE CHECK FAILED ==
-- stdout --

-- stderr --
TS2846 [ERROR]: A declaration file cannot be imported without 'import type'. Did you mean to import an implementation file './types.ts' instead?
import { Flag, Command, Selective } from "./types.d.ts";
                                         ~~~~~~~~~~~~~~
    at file://<tmpdir>/src/main.ts:1:42

error: Type checking failed.

